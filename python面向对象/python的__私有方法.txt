



****
在class里面定义的以 __开头的变量和函数,都只能在class内部访问使用,此外都访问不到.
因为Python会自动把__开头的变量和函数替换成_className__开头的名字,
例如:
class A:
    __a = 1
    def __b(self):
        pass

__a 会变成 _A__a
__b() 会变成 _A__b()

所以在其他地方直接访问__开头的变量和函数就访问不到了,
其实如果一定要强制访问的话,也可以通过_className__XX这样的方式来访问的...



